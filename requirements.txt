# 核心依赖
ultralytics>=8.1.0
opencv-python>=4.8.0
numpy>=1.24.0
torch>=2.0.0
pyyaml>=6.0

# UI显示（PyQt5）
PyQt5>=5.15.0

# Hailo-8支持（可选，仅在树莓派+Hailo-8环境下需要）
# hailo-platform>=4.15.0

# ONNX支持（CPU模式需要）
onnxruntime>=1.16.0

# 语音提示（Linux系统）
# espeak 或 festival（通过系统包管理器安装）

# Windows语音支持（可选）
# pywin32>=300
